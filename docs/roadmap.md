# Roadmap

This roadmap decides what to do next. Work on the first incomplete item in `Now` unless there is an explicit product decision to change priority.

Status values:

- `not-started`
- `in-progress`
- `blocked`
- `done`

## Now

| Order | Status | Item | Spec | Notes |
| --- | --- | --- | --- | --- |
| 1 | done | Search sources | `docs/specs/001-search-sources.md` | Configure and list Vinted catalog URLs from API/PWA. |
| 2 | done | Vinted catalog research | `docs/specs/002-vinted-catalog-research.md` | Discover how catalog data is delivered and define provider contract. |
| 3 | done | Manual runs | `docs/specs/003-manual-run.md` | Trigger source execution manually and record run state. |
| 4 | done | Item persistence | `docs/specs/004-item-persistence.md` | Store normalized public catalog items. |
| 5 | done | Fast detection and seen tracking | `docs/specs/005-deduplication-and-opportunities.md` | Use fast catalog JSON, catalog identity, monitor traceability, and bounded detail fetch. |
| 6 | done | Bounded concurrent scheduler and runtime cache | `docs/specs/008-scheduler.md` | Run sources concurrently with limits, jitter, isolated anonymous sessions, and global item cache before alerting. |
| 7 | done | Results and opportunities browser | `docs/specs/007-opportunities-table.md` | Paginated results, source scrape traceability, filters, mobile cards, and separate tabs before creating opportunities. |
| 8 | done | Frontend structure baseline | `docs/development.md` | Split the PWA into layout, feature, shared component, helper, hook, and style modules before adding local filters. |
| 9 | done | Session exclusion filters, monitor, and proxy pool | `docs/specs/006-local-filters.md` | Launch monitor sessions with monitor-owned exclusion terms, opportunities, run monitor, and encrypted proxy profiles. |
| 10 | done | Source archive, time windows, and timed sessions | `docs/specs/001-search-sources.md`, `docs/specs/008-scheduler.md` | Archive sources safely, configure one daily time window with timepickers, and launch bounded sessions from now. |
| 11 | done | Opportunity monitors model correction | `docs/specs/001-search-sources.md`, `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/006-local-filters.md`, `docs/specs/008-scheduler.md` | Treat configured Vinted searches as reusable monitors with per-monitor dedupe, optional filters, and accumulated monitor metrics. |
| 12 | done | Professional monitor logs | `docs/specs/008-scheduler.md`, `docs/security.md` | Structured run events with levels, safe session diagnostics, durations, Redis/cache events, and PWA log timeline. |
| 13 | done | Producer-Consumer + DataDome bypass | `docs/specs/010-producer-consumer-bypass.md` | Prepared residential session, catalog baseline, reliable queue recovery and five-item public-detail run verified live on 2026-07-11. |
| 13.1 | done | Prepared session hardening | `docs/specs/010-producer-consumer-bypass.md` | Require strict prepared context (`datadome`, `__cf_bm`, CSRF, anon, access, `v_udt`, geo/locale/screen) before a monitor-owned session becomes reusable. |
| 13.2 | done | Public item document enrichment | `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/010-producer-consumer-bypass.md` | Structural JSON-LD/Next Flight parsing, resilient Redis retries, public availability/pricing, complete direct-CDN photos, production-path detail probe, and accessible opportunity gallery verified against the supplied HAR plus backend/PWA tests. |
| 13.3 | done | Fast item detail pipeline | `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/010-producer-consumer-bypass.md` | Separate timings, recent sticky egress reuse, selective Flight parser and safe early-rejection shadow shipped; C2 remains canary-only because the live persistent C1 control was faster. |
| 14 | done | Fast opportunity pipeline with Redis seen cache | `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/006-local-filters.md`, `docs/specs/007-opportunities-table.md`, `docs/specs/008-scheduler.md` | Make Redis mandatory for monitor seen state, persist only opportunities as product results, and remove seen-results/session legacy. |
| 14.1 | done | Notification-ready opportunity contract | `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/006-local-filters.md` | Description-only filtering, versioned Redis policy, optional catalog views, safe single-request early rejection and redacted diagnostics independently audited before notification delivery. |
| 14.2 | done | Practical SDD workflow governance | `AGENTS.md`, `docs/sdd-process.md`, `docs/adr/0002-sdd-workflow.md` | Granular planning branches, one-task implementation branches, confirmation/integration gates, proportional integration-first acceptance, bounded automatic audits and fail-stop defaults documented; semantic checks and three-pass independent audit completed. |
| 14.3 | done | Atomic initial scheduler admission | `docs/specs/008-scheduler.md` | Transaction-scoped advisory lock and one-commit activation shipped; forced-overlap PostgreSQL race produced one `201`/one `409` with exact winner/loser state, rollback left no residue, focused gates and independent audit passed without external traffic. |
| 14.4 | done | Scheduler producer availability | `docs/specs/008-scheduler.md`, `docs/deployment.md` | Producer-owned heartbeat, fail-closed API/PWA availability and mutation-free `503` verified with PostgreSQL, the live worker/API/PWA, 397 backend tests and an independent audit; no external traffic. |
| 14.5 | done | Scheduler watchdog fail-stop | `docs/specs/008-scheduler.md`, `docs/deployment.md` | Producer stall caused real worker self-exit/Compose restart; Redis-down watchdog stopped only the QA recurring monitor, persisted session/event state and later drained the inactive task without HTTP. API-owned migration gates, invalid-config/error exits, 407 backend tests and independent audit passed. |
| 14.6 | done | Real recurring cadence closure | `docs/specs/008-scheduler.md`, `docs/010-producer-consumer-implementation.md` | Playwright launch produced one immediate and two scheduler successes at bounded 60-second cadence: initial deadline +62 s, later starts +63.154 s and +60.453 s, exactly one catalog request each. Full QA graph/Redis/runtime configuration removed and restored. |
| 14.7 | done | Transactional SSE outbox | `docs/specs/008-scheduler.md`, `docs/data-model.md` | Alembic 0017, atomic event/outbox publication and repeatable tail fencing passed isolated migrations, 23 focused/418 backend tests, real 1005-event tail/restart QA and three audit passes. No external traffic or QA residue. |
| 14.8 | done | Persisted run-event redaction parity | `docs/specs/008-scheduler.md`, `docs/security.md` | Strict immutable/canonical markers and identical JSONB/REST/SSE output passed hostile-metadata QA, 38 focused/428 backend tests and independent audit/recheck. No external traffic or QA residue. |
| 14.9 | done | PWA EventSource reconnect race | `docs/specs/008-scheduler.md`, `docs/development.md` | Real API restart, deterministic late-callback probe and one backend-persisted event proved one current stream, inert stale callbacks and cursor resume with one render. Ruff, PWA lint/build, cleanup, full `ba4b9cc` equivalence and independent audit passed without external traffic. |
| 14.10 | done | Service lifecycle map | `docs/architecture.md`, `docs/deployment.md` | Dev/prod Compose render, real startup/Alembic order, Redis/PostgreSQL/API failures, restart behavior and API SIGTERM timeout were mapped; exact runtime state, safe QA/migration runbooks and independent audit passed without product changes or external traffic. |
| 14.11 | done | Monitor command map | `docs/architecture.md`, `docs/specs/001-search-sources.md` | Live Playwright create/config-edit/archive plus API identity edit returned `201/200/204`; invalid UI/API requests, archived PATCH `404` and repeated DELETE `204` were mutation-free/idempotent. The fresh QA graph stayed empty and PostgreSQL counts/sequence, Redis 9-key state, heartbeat, services and Vite were restored. 38 focused tests, Ruff, PWA lint/build and three audit passes passed; discovered gaps are split into 14.25-14.31. No external traffic. |
| 14.12 | done | Public anonymous session map | `docs/architecture.md`, `docs/security.md`, `docs/spec.md`, `docs/specs/008-scheduler.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/data-model.md`, `docs/deployment.md`, `docs/adr/0006-curl-cffi-tls-bypass.md`, `docs/research/vinted-catalog.md` | Read-only API/PWA/PostgreSQL proved the latest `ready` label while all 9 ready rows were expired; Redis stayed empty, worker/watchdog stopped and host Vite unchanged. Seven deterministic provider tests, Ruff, PWA lint/build, reference/diff checks and independent audit passed. Product gaps are split into 14.12.1-14.12.12 plus archive race 14.30. No QA data or external traffic. |
| 14.12.1 | done | Private API access control | `docs/specs/011-local-pwa-access-control.md`, `docs/adr/0007-postgresql-local-user-sessions.md`, `docs/architecture.md`, `docs/spec.md`, `docs/security.md`, `docs/deployment.md`, `docs/development.md`, `docs/data-model.md`, `docs/specs/010-producer-consumer-bypass.md` | Opaque PostgreSQL sessions, Argon2, exact Origin/CSRF, protected REST/SSE, PWA auth gate and same-origin deployment passed isolated migrations, 44 focused/450 full tests, real DB outage, production headers and Playwright restart/logout/liveness QA. Cleanup left no QA state or external traffic. |
| 14.12.2 | done | Proxy/session identity and eligibility fence | `docs/architecture.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/security.md`, `docs/data-model.md`, `docs/deployment.md`, `docs/adr/0006-curl-cffi-tls-bypass.md`, `docs/research/vinted-catalog.md` | Counter+keyed identity token, generation-bound sessions/tasks and shared/exclusive pre-provider fences passed real PostgreSQL/Redis/FastAPI races, a fresh worker-process template restart, both adversarial deadlock cases, isolated Alembic zero/0018/0019/downgrade paths, 491 backend tests, Ruff and PWA lint/build. Stale work made zero local provider calls; QA state was empty before removal and no external traffic occurred. |
| 14.12.3 | not-started | Catalog response fail-stop contract | `docs/architecture.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/research/vinted-catalog.md` | After 14.12.2; branch `fix/catalog-response-fail-stop`; Cloudflare/DataDome propagate as challenges, and rejection/rate-limit recovery occurs only on explicitly contracted same-identity paths. |
| 14.12.4 | not-started | Prepared-session write durability | `docs/architecture.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/data-model.md` | After 14.12.3; branch `fix/prepared-session-write-durability`; catalog/probe cookie rotations, refreshes and invalidations persist coherently under one source/row/sticky fence, including failed retries and stale writes. |
| 14.12.5 | not-started | Canonical prepared-session eligibility | `docs/architecture.md`, `docs/security.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/data-model.md`, `frontend/src/features/settings/SettingsView.tsx` | After 14.12.4; branch `fix/prepared-session-eligibility`; runtime/API/PWA agree on viewport, expiry, exhaustion, canonical ready row and automatic lifecycle refresh. |
| 14.12.6 | not-started | APP secret-key startup sentinel | `docs/architecture.md`, `docs/security.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/deployment.md`, `docs/data-model.md` | After 14.12.5; branch `fix/app-secret-key-sentinel`; detect a global `APP_SECRET_KEY` mismatch before API/worker serve without rewriting encrypted rows. |
| 14.12.7 | not-started | Encrypted-row corruption handling | `docs/architecture.md`, `docs/security.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/deployment.md`, `docs/data-model.md` | After 14.12.6; branch `fix/encrypted-row-corruption`; one corrupt session context or proxy password stops only its owner visibly and non-destructively. |
| 14.12.8 | not-started | Proxy credential public contract | `docs/architecture.md`, `docs/security.md`, `docs/data-model.md`, `docs/deployment.md`, `docs/specs/008-scheduler.md`, `docs/specs/010-producer-consumer-bypass.md`, `backend/src/vinted_monitor/api/schemas.py`, `frontend/src/api.ts` | After 14.12.7; branch `fix/proxy-credential-read-model`; raw proxy username/password never appear in storage/read responses that promise encrypted or masked credentials. |
| 14.12.9 | not-started | Durable reusable-session acquisition | `docs/architecture.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/data-model.md` | After 14.12.8; branch `fix/prepared-session-reuse-ledger`; reuse remains counted exactly once across run rollback, crash and redelivery before provider traffic, with unambiguous API/data names. |
| 14.12.10 | not-started | Durable preparation attempt | `docs/architecture.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/010-producer-consumer-implementation.md`, `docs/data-model.md` | After 14.12.9; branch `fix/prepared-session-prepare-ledger`; preparation has one durable owner before egress/bootstrap/probe and reconciles accepted/failure state exactly once. |
| 14.12.11 | not-started | Anonymous session secret retention | `docs/architecture.md`, `docs/security.md`, `docs/data-model.md`, `docs/deployment.md`, `docs/specs/010-producer-consumer-bypass.md` | After 14.12.10; branch `fix/anonymous-session-retention`; expire, exhaust, incomplete and stop-limit outcomes receive an explicit bounded purge/retention policy without deleting in-flight preparation, including sliding versus absolute TTL. |
| 14.12.12 | not-started | Obsolete session contract removal | `docs/specs/010-producer-consumer-bypass.md`, `backend/src/vinted_monitor/api/main.py`, `backend/src/vinted_monitor/worker/consumer.py` | After 14.12.11; branch `refactor/remove-proxy-session-tombstones`; delete pre-production `410` adapters, stale consumer documentation and their tests/OpenAPI surface. |
| 14.13 | not-started | Run lifecycle map | `docs/architecture.md`, `docs/specs/008-scheduler.md` | After 14.12.12; branch `docs/run-lifecycle-map`; activate/stop plus manual, recurring, duration, window, deadlines, terminal states and compensation. |
| 14.14 | not-started | Queue recovery map | `docs/architecture.md`, `docs/data-model.md`, `docs/specs/010-producer-consumer-bypass.md` | After 14.13; branch `docs/queue-recovery-map`; ready/reserved/processing/ACK/requeue/dead-letter and AOF recovery. |
| 14.15 | not-started | Cache and concurrency map | `docs/architecture.md`, `docs/data-model.md`, `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/008-scheduler.md` | After 14.14; branch `docs/cache-concurrency-map`; seen/processing/retry/finalizing ownership, locks and capacity. |
| 14.16 | not-started | Persistence, events and logs map | `docs/architecture.md`, `docs/data-model.md`, `docs/deployment.md`, `docs/security.md` | After 14.15; branch `docs/persistence-events-logs-map`; SQL transactions, outbox/SSE, stdout, redaction, PWA and retention. |
| 14.17 | not-started | Current-state documentation pruning | `docs/architecture.md`, `docs/deployment.md`, `docs/data-model.md`, `docs/security.md`, `docs/010-producer-consumer-implementation.md`, `docs/specs/001-search-sources.md`, `docs/specs/005-deduplication-and-opportunities.md`, `docs/specs/008-scheduler.md`, `docs/specs/010-producer-consumer-bypass.md` | After 14.16; branch `docs/current-state-pruning`; remove confirmed stale blockers and superseded descriptions without expanding to unrelated docs. |
| 14.18 | not-started | Deterministic integration-test settings and cleanup | `backend/tests/test_manual_runs.py`, `docs/development.md` | After 14.17; branch `fix/integration-test-settings`; remove host `.env`/encryption-key dependence and leaked scheduler settings/proxy telemetry from integration tests without weakening production contracts. |
| 14.19 | not-started | Dependency-aware API and worker readiness | `docs/deployment.md`, `docs/specs/008-scheduler.md` | After 14.18; branch `fix/dependency-aware-readiness`; separate process liveness from DB/Redis readiness so Redis loss cannot leave worker/scheduler advertised available. |
| 14.20 | not-started | API graceful shutdown | `docker-compose.yml`, `docker-compose.prod.example.yml`, `docs/deployment.md` | After 14.19; branch `fix/api-graceful-shutdown`; give Uvicorn direct signal ownership and a bounded verified grace without changing API contracts. |
| 14.21 | not-started | Worker graceful drain and reservation recovery | `docs/specs/008-scheduler.md`, `docs/data-model.md`, `docs/deployment.md` | After 14.20; branch `fix/worker-graceful-drain`; stop admission/reservation, bound in-flight completion and recover ambiguous Redis work exactly once. |
| 14.22 | not-started | Watchdog observability | `docs/specs/008-scheduler.md`, `docs/deployment.md` | After 14.21; branch `fix/watchdog-observability`; make crash and hang/loss-of-progress distinguishable and health-visible without changing fail-stop ownership. |
| 14.23 | not-started | Core service restart ownership | `docs/deployment.md`, `docker-compose.yml`, `docker-compose.prod.example.yml` | After 14.22; branch `fix/core-service-restart-policy`; decide and verify restart responsibility for API, PostgreSQL, Redis and frontend independently of readiness. |
| 14.24 | not-started | Manual diagnostic secret safety | `docs/security.md`, `docs/development.md` | After 14.23; branch `fix/manual-diagnostic-secret-safety`; remove raw proxy/header output, contain generated browser references and require explicit traffic budgets. |
| 14.25 | not-started | Monitor edit/run exclusion | `docs/specs/001-search-sources.md`, `docs/specs/003-manual-run.md`, `docs/specs/008-scheduler.md` | After 14.24; branch `fix/monitor-edit-run-exclusion`; serialize PATCH/Guardar with every non-terminal manual or recurrent run instead of treating `is_active=false` as fully stopped. |
| 14.26 | not-started | PWA monitor identity editing | `docs/specs/001-search-sources.md` | After 14.25; branch `feature/pwa-monitor-identity-edit`; edit scheduler-inactive monitor name/URL in the detail, preserve ID/history and expose current API validation without launching work. Name-length/status hardening belongs to 14.29. |
| 14.27 | not-started | PWA monitor command-state honesty | `docs/specs/001-search-sources.md`, `docs/development.md` | After 14.26; branch `fix/pwa-monitor-command-state`; allow one mutation command at a time, distinguish commit from failed derived refresh, reconcile from API and remove all local state of an archived ID. |
| 14.28 | not-started | PWA monitor bootstrap isolation | `docs/development.md` | After 14.27; branch `fix/pwa-monitor-bootstrap-isolation`; load sources independently so an opportunities/runs/proxies failure cannot hide valid monitors, with explicit per-surface errors. |
| 14.29 | not-started | Search-source persistence invariants | `docs/specs/001-search-sources.md`, `docs/data-model.md` | After 14.28; branch `fix/search-source-invariants`; reject names beyond `VARCHAR(160)` with `422`, default direct SQL rows inactive and advance `updated_at` on real PATCH changes. |
| 14.30 | not-started | Archive runtime-state closure | `docs/architecture.md`, `docs/specs/001-search-sources.md`, `docs/specs/008-scheduler.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/data-model.md` | After 14.29; branch `fix/archive-runtime-closure`; clear every runtime marker, give reserved/running work one explicit terminal ownership and prevent provider preparation after archive wins the source fence. |
| 14.31 | not-started | Archive queue-cleanup convergence | `docs/specs/001-search-sources.md`, `docs/specs/010-producer-consumer-bypass.md`, `docs/data-model.md` | After 14.30; branch `fix/archive-queue-convergence`; converge both Redis-failure/SQL-success and Redis-success/SQL-failure, with visible retry or compensation and idempotent repair. |
| 14.32 | not-started | Local login abuse controls | `docs/specs/011-local-pwa-access-control.md`, `docs/security.md`, `docs/deployment.md` | After 14.31; branch `fix/local-login-abuse-controls`; bound repeated local-login failures durably without account enumeration, implicit unlock fallback or dependence on process memory. |
| 14.33 | not-started | PWA browser security headers | `docs/specs/011-local-pwa-access-control.md`, `docs/security.md`, `docs/deployment.md`, `frontend/nginx.conf` | After 14.32; branch `fix/pwa-browser-security-headers`; deploy and verify a restrictive CSP plus document/security headers without breaking the same-origin PWA/API/SSE path. |

### Operational closure contracts

`fix/scheduler-worker-liveness-sse-hardening` at `ba4b9cc` was reviewed as a read-only patch source, not a merge candidate. The 14.9 equivalence check found every product hunk integrated or superseded by 14.3-14.9; the remaining `qa-pwa.ps1 -SkipWorker` helper was rejected because it neither stops nor proves the absence of an existing worker. The local reference branch has no remaining merge candidate and can be deleted after the 14.9 closure commit.

| Item | Affected state and acceptance | Traffic, cleanup and exclusions |
| --- | --- | --- |
| 14.3 | FastAPI and PostgreSQL `search_sources`, `monitor_sessions` and `runs`. A PostgreSQL advisory lock serializes initial admission. Two concurrent starts at capacity one yield one winner and one `409`; activation, session, `next_run_at` and initial run are atomic. A pre-run failure leaves no active/session/deadline/run residue. Manual, duration and window behavior remains valid. | Real PostgreSQL with the Vinted provider controlled only at the external boundary. Delete QA sources, sessions, runs, events and Redis markers. Excludes heartbeat, watchdog and SSE. |
| 14.4 | Worker producer heartbeat in `app_settings`; public `GET /api/scheduler` adds `worker_available: boolean` and `worker_last_seen_at: UTC or null`, and `effective_enabled` requires a fresh producer. Missing, stale, malformed, future or unreadable state is unavailable; recurring start returns mutation-free `503`. PWA status/preflight blocks launch, and an API refresh error cannot retain a usable stale state. | Start/stop the real worker only after confirming no recurring monitor is active; verify API and PWA with Playwright, then restore initial services/config. No Vinted/proxy. Excludes worker restart and watchdog. |
| 14.5 | Invalid scheduler configuration or producer loss of progress makes the worker self-exit so Compose can restart it. The watchdog stops only recurring monitors, leaves manual monitors intact, rechecks heartbeat after source locks, closes sessions and records the failure. API remains the migration owner; worker/watchdog wait for API health. Unexpected watchdog errors terminate instead of looping silently. | Use a local QA source/session/task fixture and real containers. PostgreSQL inactive state is authoritative if Redis cleanup fails visibly. Restore service state and remove all QA state. No Vinted/proxy. |
| 14.6 | Playwright launches one recurring monitor through the live PWA/API/worker/PostgreSQL/Redis/events path. With interval 60 and jitter 10%, initial `next_run_at` is activation +60..66 seconds; exactly one immediate and two later terminal runs occur without duplicate cadence. | One copied public monitor URL, one healthy configured proxy, no direct fallback, `catalog_per_page=1`, detail candidates `0`, external retries disabled, one explicit baseline recalibration that may prepare one anonymous session internally, and three business runs total across all attempts. Stop after run three and remove the full QA graph/keys/queues; extra live budget requires new authorization. |
| 14.7 | Alembic 0016-to-0017 and zero-to-head, PostgreSQL event/outbox/publication transactions and the real API stream. Commit, rollback, inverted commits, historical backfill, restart, tail, backlog and resume give every committed monitor event exactly one durable publication cursor and duplicate-free logical resume; SSE transport may reconnect or replay. | Prefer an isolated QA database and drop it afterwards; otherwise delete QA events/outbox/publications/source by captured IDs. No Vinted/proxy. Excludes redaction changes and frontend races. |
| 14.8 | Persisted JSONB event details loaded through real REST and SSE. Legitimate safe markers match; caller-forged shapes and raw canaries never appear in response, DB-visible output or logs. | Synthetic secret canaries only; delete QA event/publication/outbox/run/source. No Vinted/proxy. Excludes EventSource ownership. |
| 14.9 | Live PWA, API and SSE connection state. API restart keeps at most one current stream and one reconnect timer; a prolonged outage may cause sequential closed attempts. Closed-instance callbacks are ignored; navigation closes and return resumes from the last cursor with one rendered event. | Use a backend-produced local operational event, not browser injection. Restore API/PWA and remove QA event state. No Vinted/proxy. Finish with a hunk-by-hunk `ba4b9cc` equivalence decision. |
| 14.10 | Compose services and their startup/migration/health/restart ownership, verified with `config`, `up`, `ps`, controlled kill/restart and logs. The output is one current decision tree in architecture/deployment. | No Vinted/proxy. Capture and restore initial service state. A runtime discrepancy becomes a later fix; this documentation branch does not change services. |
| 14.11 | Monitor commands through the live PWA/API with PostgreSQL confirmation: valid create/edit/archive and invalid requests with no mutation. | No Vinted/proxy and no monitor launch. Remove the QA monitor graph. Session preparation belongs to 14.12; activation, stop and run execution belong to 14.13. |
| 14.12 | Public anonymous session selection, preparation, reuse, refresh, expiry, challenge and invalidation, including durable/transient secret ownership and fail-stop decisions. Current code, API/PWA read model and PostgreSQL metadata agree with the map or every discrepancy has one bounded roadmap owner. | Reuse captured 14.6 evidence, deterministic provider tests and read-only local API/PostgreSQL/PWA state. No session/run action, Vinted/proxy call or QA row; preserve worker/watchdog and host Vite state. |
| 14.12.1 | Every `/api`/SSE business read and mutation requires an active local user plus an unexpired, unrevoked opaque PostgreSQL session. Public runtime scope is only health, auth bootstrap/login and valid CORS preflight; diagnostics OpenAPI exist only in dev/test. Login rotates the pre-auth token; logout revokes server state. Cookie flags are host-only `HttpOnly; SameSite=Strict; Path=/api` plus `Secure` outside dev/test. Exact Origin and session-bound CSRF guard every mutation. SSE revalidates and closes within 15 s after revocation/expiry/user disable; named heartbeat plus a 22.5 s PWA liveness watchdog prevent silent zombie streams, and the PWA never mounts or reconnects business state before auth. Development ports bind loopback and production exposes only same-origin Traefik routes with docs disabled. | Real PWA/API/PostgreSQL login/logout plus structured cookie/header checks that never print credentials in development and isolated production mode. Verify the full route matrix, fixation, absolute expiry, inactive user, DB failure, API restart, CSRF/Origin mutation-free rejection, unauthenticated/revoked SSE, idle heartbeat and authenticated single reconnect. Run zero-to-head and 0017-to-head migrations. Do not invoke prepare, run, proxy test or external traffic. Remove test user/sessions/state and restore services. Rate limiting/CSP are explicitly 14.32/14.33. |
| 14.12.2 | A versioned effective proxy identity covers scheme, host, port, username, password, country-derived preset and sticky template. Any mismatch prevents stale selection and purges/invalidates affected rows under serialized ownership. A queued/manual run revalidates profile existence, `is_active`, cooldown, country and identity generation immediately before provider construction; stale captured work makes zero provider calls, while a fresh post-change command may prepare only the new identity. | Parameterized real API/PostgreSQL/Redis races mutate host, port, username, password, country/preset, active/cooldown and `PROXY_STICKY_USERNAME_TEMPLATE` (with controlled settings restart) between capture and provider barrier for both manual and queued work. Use a local call counter/safe fingerprints; assert zero stale calls, then restore settings/profile/telemetry and delete sessions/events/keys. No Vinted/proxy. |
| 14.12.3 | The first Cloudflare/DataDome signal is terminal for the task: it fails the run, reaches consumer challenge handling, invalidates any acquired/persisted session and ACKs without requeue or another provider attempt. Before 14.12.10 introduces a pre-traffic durable attempt owner, a preparation challenge records a safe failed run and leaves no reusable session. Anonymous rejection and `429` follow one explicit decision tree; missing/invalid `Retry-After` does not invent a fallback unless separately authorized. | Inject local fake HTTP responses through the production provider, real run persistence and Redis consumer boundary. Assert exactly one challenged provider attempt, session invalidation or safe absence, terminal ACK/no ready copy and zero later provider calls across the former retry budget. No external traffic. |
| 14.12.4 | Cookie/context rotation from catalog, probe and detail is detected uniformly. Creation, refresh and invalidation acquire the monitor row themselves; a successful same-identity refresh is persisted only to the same ready row after revalidating row status and sticky identity. A later failure either keeps the verified refresh by contract or invalidates, never leaves a falsely reusable stale row. | Production provider with local fake responses plus real PostgreSQL ciphertext/fingerprint checks using canaries that are never printed. Race challenge invalidation against refresh after rollback; remove test rows. No Vinted/proxy. |
| 14.12.5 | One backend eligibility decision includes viewport, profile, proxy identity, TTL, budget and complete context. Explicit preparation has a defined canonical replacement/pool policy, and API/PWA show `usable_now` plus reason and refresh it after automatic terminal changes. | Real API/PostgreSQL rows for ready/expired/exhausted/incompatible/multi-ready plus one production run terminal through a local fake network boundary and real SSE. Live Playwright verifies Settings before/after without reload. No external traffic; delete rows/runs/events/keys and restore services/Vite. |
| 14.12.6 | A durable non-secret verification sentinel detects a global `APP_SECRET_KEY` mismatch covering both encrypted stores before API routes or worker consumers become available. A truly empty encrypted store may initialize it atomically. If the sentinel is absent/corrupt while any encrypted row exists, startup fails and never creates/regenerates it as fallback, scans rows for recovery, rewrites ciphertext or prepares replacements. | Isolated migrated databases cover empty first initialization, legacy ciphertext with no sentinel, corrupt sentinel and key A ciphertext/sentinel started with key B. Assert route/consumer readiness only for the empty bootstrap, unchanged ciphertext fingerprints and no secret output; drop isolated state. No external traffic. |
| 14.12.7 | With a valid sentinel/key, one invalid session ciphertext/JSON or proxy-password ciphertext receives an explicit unusable/corrupt state for its monitor/profile. The dependent operation stops visibly without retry loop, automatic replacement, proxy penalty or ciphertext rewrite; unrelated owners remain usable. | Isolated migrated database plus real API/worker reads with one corrupt row of each type and untouched controls. Assert safe diagnostics and ciphertext fingerprints, then drop isolated state. No provider call or external traffic. |
| 14.12.8 | Proxy username/password are encrypted at rest where classified as credentials and API/PWA expose only explicit masked/presence fields. POST/GET/PATCH responses and logs never contain username/password canaries. | Isolated zero-to-head and existing-row upgrade plus real API/PWA edit/read path with local canaries. PostgreSQL contains ciphertext decryptable only with the correct key and no raw canary; API/PWA/logs expose only mask/presence. Clean isolated state exactly. No proxy test or external traffic. |
| 14.12.9 | Reusing a prepared session creates one idempotent acquisition tied to the run/task and commits its logical use before provider traffic. Run rollback, process death and redelivery cannot erase or duplicate it; exhaustion remains fail-stop. Because this slice changes the use model, database, API, PWA and events update atomically to logical-use terminology and remove the misleading request-count development contract. | Seed an eligible local session, then exercise a real API manual run and Redis/PostgreSQL worker task against a local provider-call barrier with forced rollback and worker termination/restart. Assert count/ownership before release, verify terminology with live Playwright, clean rows/events/keys and restore worker/Vite state. No Vinted/proxy. |
| 14.12.10 | Explicit and automatic preparation persist one idempotent attempt owner before egress/bootstrap/probe. Failure/crash remains visible and redelivery resumes or terminates by contract; an accepted session receives initial logical use exactly once without duplicating the attempt. | Real PWA explicit prepare plus Redis/PostgreSQL worker automatic path through a local fake provider with barriers before/after bootstrap and worker termination/restart. Assert rows/events/use ownership, remove QA graph/keys and restore services/Vite. No Vinted/proxy. |
| 14.12.11 | `incomplete`, expired, exhausted, invalid and stop-limit outcomes have explicit retention windows and purge ownership; sliding refresh TTL versus an absolute lifetime is an explicit decision. Cleanup is idempotent, locked and cannot delete an eligible context or an in-flight `preparing` attempt. | Real PostgreSQL lifecycle/preparation rows and cleanup job/command with encrypted canaries checked only for presence/absence. No provider traffic; delete QA rows and restore sequence/services if touched. |
| 14.12.12 | Proxy-owned preflight/catalog-probe/import tombstones are absent from routing and OpenAPI; monitor-owned preparation/detail remain the only current contracts. Stale no-auto-prepare comments, legacy tests and types are removed. | Route/OpenAPI and focused consumer regression checks only; no services need restart and no Vinted/proxy/state mutation. |
| 14.13 | Activation, stop, manual, continuous, duration and window run transitions, deadline authority, terminal states, compensation and failure ownership reconciled with code/spec. | Reuse sanitized IDs/timestamps from 14.6 and exercise negative paths locally. No additional Vinted/proxy traffic and no product-code changes. |
| 14.14 | Redis ready/reserved/processing/ACK/requeue/dead-letter ownership and AOF recovery verified with a real worker/Redis restart and bounded QA payloads. | No Vinted/proxy. Remove QA queue, processing, reverse-marker and dead-letter entries and restore worker/Redis state. |
| 14.15 | Redis/PostgreSQL seen, processing, retry, `finalizing`, policy hash, admission locks and capacity mapped from real state transitions. | Reuse 14.6 hit/skip evidence and local controlled failures; no new external traffic. Delete QA cache/retry/lock residue. |
| 14.16 | PostgreSQL transaction boundaries, event outbox/publication, SSE/PWA delivery, process logs, redaction and retention risk traced end to end. | No Vinted/proxy. Use local sanitized events, remove QA state, and classify absent rotation/retention as accepted risk or a new contained task. |
| 14.17 | Only documents changed by 14.3-14.16 are checked against captured evidence; stale blockers and superseded current-state prose are removed or marked historical where durable. | Documentation-only semantic/link audit. No services or external traffic; unrelated drift becomes separate roadmap work. |
| 14.18 | Integration tests use explicit settings regardless of host `.env`, restore `app_settings` and pre-existing proxy telemetry after mutation, and the isolated sticky prepared-session test passes without changing production encryption/session validation. | No Vinted/proxy. Use only test-owned settings/rows and restore or remove them through fixtures; exclude runtime session behavior changes. |
| 14.19 | API exposes honest liveness/readiness boundaries and worker/scheduler availability becomes false or fail-stops when Redis cannot enqueue/consume; PostgreSQL, Redis and migration state are checked at the correct gate. | Real local dependency outages with zero active monitors/queues, exact state restoration and no Vinted/proxy. Excludes signal/drain and restart-policy changes. |
| 14.20 | Both Compose API entrypoints deliver SIGTERM to Uvicorn, complete shutdown within a declared grace and restart through API-owned Alembic without exit `137`. | Stop/start the real API with worker/watchdog stopped; restore API/host Vite and make no Vinted/proxy request. Excludes worker draining and restart policy. |
| 14.21 | SIGTERM stops producer/admission and new Redis reservations, gives bounded in-flight work an explicit outcome, and recovers an ambiguous reservation exactly once on the next worker start. | Real worker/Redis/PostgreSQL with a local non-HTTP task fixture; restore services, heartbeat and queue keys. No Vinted/proxy. Excludes watchdog health and core restart policy. |
| 14.22 | Watchdog crash, hang or loss of progress is externally distinguishable; its health/supervision detects the bounded fault while retaining recurring-only DB-first fail-stop semantics. | Real watchdog/PostgreSQL with zero active monitors or a test-owned local source; restore heartbeat/service state and remove QA rows. No Vinted/proxy. Excludes worker drain and other service restart policies. |
| 14.23 | API, PostgreSQL, Redis and frontend each have an explicit restart owner and bounded real-outage recovery result; manual stop remains intentional and does not silently resume execution. | Real local container exits with worker/watchdog and queues stopped; restore exact service/volume/host-Vite state. No Vinted/proxy. Excludes readiness semantics and graceful worker drain. |
| 14.24 | Manual diagnostics never print raw proxy userinfo or captured secret headers, generated browser-reference artifacts are ignored/sanitized, and every external probe requires an explicit bounded allowance. | Static canaries and mocked/local output by default. No live Vinted/proxy call without separate user authorization. |
| 14.25 | PATCH and Guardar are unavailable while any run for the monitor is non-terminal, and a forced PATCH-vs-run race has one serialized winner without changing the configuration seen by the run. | Real API/PWA/PostgreSQL with a local non-HTTP run fixture. Restore rows and sequence; no Vinted/proxy. Excludes activation/stop redesign and identity fields. |
| 14.26 | A scheduler-inactive monitor exposes name and URL editing in the live PWA; a valid save retains ID/history and recalculates normalized query/compatibility, while invalid URL input is visible and mutation-free. | Playwright, API and PostgreSQL on one fresh inactive QA monitor. No session/run action or external traffic; remove its complete graph and Redis keys. Excludes cadence/filter redesign and name-length hardening. |
| 14.27 | Rapid submit produces one command; after a confirmed create/archive, a failed stats/runtime refresh cannot be reported as if the mutation failed, reload converges to server state and no state keyed by an archived ID remains in memory. | Real PWA/API with controlled local response failures and PostgreSQL counts. No Vinted/proxy; remove QA source state. Excludes initial multi-surface bootstrap. |
| 14.28 | Monitors render from a successful source response even when opportunities, runs or proxies fail independently; each failed surface remains honest and can retry without discarding successful data. | Playwright against the real API with one dependency response faulted locally at a time. Restore responses/state; no external traffic. Excludes command mutation handling. |
| 14.29 | Names longer than 160 characters return mutation-free `422`; ORM and database defaults cannot silently activate a new source; a successful changed PATCH advances `updated_at` exactly once. | Real API/PostgreSQL plus isolated migration/default verification. Restore sequence and delete QA rows; no Vinted/proxy. Excludes PWA fields and archive lifecycle. |
| 14.30 | Archive clears `monitor_started_at`, deadlines and session ownership and defines a deterministic outcome for a source task already reserved or running; no later success can make the archived monitor look active. If archive wins the row fence, the session selector returns an archive-specific terminal result and never calls bootstrap/probe/provider preparation. | Real local worker/PostgreSQL/Redis with a non-HTTP controllable task fixture and a provider-call counter. Restore services and remove all QA rows/events/keys; no Vinted/proxy. Excludes ready-queue cleanup and Redis outage reconciliation. |
| 14.31 | Redis cancellation failure or a later SQL rollback is observable and recoverable: no archived source retains ready work and no live source silently loses it; retry/compensation converges tasks and reverse markers exactly once. | Real Redis failure plus forced SQL commit failure, each with one local ready QA task and worker stopped. Restore Redis/service state and remove queue/QA residue; no Vinted/proxy. Excludes running-task terminal semantics. |

## Next

| Order | Status | Item | Spec | Notes |
| --- | --- | --- | --- | --- |
| 15 | not-started | Notifications | `docs/spec.md` | PWA push, Telegram, webhook, Discord, or email after web monitoring works. |

## Later

| Order | Status | Item | Spec | Notes |
| --- | --- | --- | --- | --- |
| 16 | not-started | Production deployment hardening | `docs/deployment.md` | Complete `APP_HOST`/Traefik environment contract, remove stale frontend env names, and validate Traefik, Cloudflare, health and restart behavior. |

## Future Authenticated Actions

| Order | Status | Item | Spec | Notes |
| --- | --- | --- | --- | --- |
| 17 | not-started | Authenticated actions | `docs/specs/009-authenticated-actions.md` | Favorites, checkout discovery, pre-purchase, and explicit purchase. |

## Roadmap Rules

- Do not skip ahead unless the user explicitly changes priority.
- If an item changes scope, update its existing spec instead of creating a parallel document.
- If a new item is needed, add it here and create a spec only if no existing document owns it.
- Mark an item `done` only when its acceptance criteria and verification steps are satisfied.
- Keep `docs/spec.md` as the product-level summary and `docs/specs/` as feature-level specs.
- Integrate and close one 14.x task before requesting confirmation to start the next; do not batch their implementation or verification.
